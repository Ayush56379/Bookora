// Bookora Explore — single production filter bridge.
// Firebase is the only source of truth for visible category chips.
(() => {
  'use strict';
  if (window.__BOOKORA_EXPLORE_PRODUCTION_FILTER_V7__) return;
  window.__BOOKORA_EXPLORE_PRODUCTION_FILTER_V7__ = true;

  const page=()=>document.querySelector('.explore-page');
  const approvedBooks=()=>{try{const s=window.BookoraState||window.state;return typeof s?.getApprovedBooks==='function'?s.getApprovedBooks():[];}catch(_){return[];}};
  const numericRating=book=>{for(const value of [book?.rating,book?.averageRating,book?.average_rating,book?.avgRating,book?.ratingValue,book?.reviewRating,book?.review_rating]){if(value===null||value===undefined||value==='')continue;const n=Number(String(value).replace(/[^0-9.\-]/g,''));if(Number.isFinite(n))return Math.max(0,Math.min(5,n));}const total=Number(book?.ratingTotal||book?.rating_total||0),count=Number(book?.reviewCount||book?.review_count||book?.ratingsCount||0);return total>0&&count>0?Math.max(0,Math.min(5,total/count)):0;};
  const numericPrice=book=>{for(const value of [book?.sale_price,book?.salePrice,book?.price,book?.original_price,book?.originalPrice]){if(value===null||value===undefined||value==='')continue;const n=Number(String(value).replace(/[^0-9.\-]/g,''));if(Number.isFinite(n))return Math.max(0,n);}return 0;};
  const normalizeCatalog=()=>{const books=approvedBooks();if(!Array.isArray(books))return;books.forEach(book=>{book.rating=numericRating(book);if(book.review_count===undefined&&book.reviewCount!==undefined)book.review_count=Number(book.reviewCount)||0;});};
  const categoryValues=book=>{const out=[];const add=v=>{if(v===null||v===undefined||v==='')return;if(Array.isArray(v)){v.forEach(add);return;}if(typeof v==='object'){add(v.name||v.title||v.category||v.label||v.slug||v.id);return;}const text=String(v).trim().replace(/\s+/g,' ');if(text)out.push(text);};add(book?.category);add(book?.categories);add(book?.category_name);add(book?.categoryName);return[...new Set(out)];};
  const categoryKey=v=>String(v||'').trim().toLowerCase().replace(/&/g,'and').replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');
  const escapeHTML=v=>String(v??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/\"/g,'&quot;').replace(/'/g,'&#039;');

  const removeUnwantedAI=()=>{const p=page();if(!p)return;p.querySelectorAll('#bookora-ai-filter,[data-smart-filter],.smart-filter-section,.ai-smart-filter').forEach(el=>el.remove());p.querySelectorAll('.filter-section').forEach(s=>{if(String(s.textContent||'').toLowerCase().includes('ai smart filter'))s.remove();});};
  const updatePriceControls=()=>{const p=page();if(!p)return;const books=approvedBooks(),maxCatalogPrice=Math.max(0,...books.map(numericPrice));const min=p.querySelector('#filter-min-price'),max=p.querySelector('#filter-max-price'),slider=p.querySelector('#filter-price-slider');if(min){min.removeAttribute('max');min.setAttribute('inputmode','numeric');}if(max){max.removeAttribute('max');max.setAttribute('inputmode','numeric');}if(slider){const sliderMax=Math.max(1000,Math.ceil(maxCatalogPrice/100)*100,Number(max?.value||0));slider.max=String(sliderMax);slider.setAttribute('aria-label',`Maximum price up to ₹${sliderMax}`);const n=Number(slider.value);if(!Number.isFinite(n)||n>sliderMax)slider.value=String(sliderMax);}};
  const styleRatingFilter=()=>{const p=page();if(!p)return;if(!document.getElementById('bookora-explore-rating-ui-v7')){const s=document.createElement('style');s.id='bookora-explore-rating-ui-v7';s.textContent=`.explore-page .filter-rating-row{display:grid!important;grid-template-columns:20px minmax(0,1fr)!important;align-items:center!important;gap:8px!important;min-height:40px!important;padding:6px 8px!important;border:1px solid transparent!important;border-radius:9px!important;color:#334155!important;font-size:12px!important;font-weight:650!important;line-height:1.2!important}.explore-page .filter-rating-row:hover{background:#faf5ff!important;border-color:#ede9fe!important}.explore-page .filter-rating-row input{width:16px!important;height:16px!important;margin:0!important;accent-color:#7c3aed!important}.explore-page .filter-rating-row .rating-option-content{display:flex!important;align-items:center!important;justify-content:space-between!important;gap:8px!important;width:100%!important}.explore-page .filter-rating-row .rating-stars-text{color:#f59e0b!important;letter-spacing:1px!important;font-size:12px!important;white-space:nowrap!important}.explore-page .filter-rating-row .rating-threshold{color:#475569!important;font-weight:700!important;white-space:nowrap!important}.explore-page .filter-rating-row .rating-all-label{color:#475569!important;font-weight:700!important}.explore-page .book-card-rating{display:flex!important;align-items:center!important;gap:5px!important;min-height:22px!important}.explore-page .category-chips{visibility:hidden}.explore-page .category-chips.bookora-category-ready{visibility:visible}.explore-page .category-chips button.active{background:#2563eb!important;border-color:#2563eb!important;color:#fff!important;box-shadow:0 2px 8px rgba(37,99,235,.22)!important}.explore-page .category-chips button.active:hover{background:#1d4ed8!important;border-color:#1d4ed8!important;color:#fff!important}`;document.head.appendChild(s);}p.querySelectorAll('.filter-rating-row').forEach(row=>{const input=row.querySelector('input[name="filter-rating"]');if(!input||row.dataset.ratingUiVersion==='7')return;const value=Number(input.value||0);row.innerHTML='';row.appendChild(input);const c=document.createElement('span');c.className='rating-option-content';if(value===0)c.innerHTML='<span class="rating-all-label">All ratings</span>';else{const stars=value>=4.5?'★★★★★':value>=4?'★★★★☆':'★★★☆☆';c.innerHTML=`<span class="rating-stars-text">${stars}</span><span class="rating-threshold">${value.toFixed(1)} &amp; up</span>`;}row.appendChild(c);row.dataset.ratingUiVersion='7';});};

  const syncAvailableCategoryChips=()=>{const p=page();if(!p)return;const chips=p.querySelector('#explore-category-chips');if(!chips)return;const books=approvedBooks(),map=new Map();books.forEach(book=>categoryValues(book).forEach(name=>{const key=categoryKey(name);if(!key)return;if(!map.has(key))map.set(key,{name,count:0});map.get(key).count++;}));const categories=[...map.values()].sort((a,b)=>a.name.localeCompare(b.name,undefined,{sensitivity:'base'}));const query=new URLSearchParams((window.location.hash||'#/explore').split('?')[1]||'');const selectedKey=categoryKey(query.get('category')||'');const signature=JSON.stringify([selectedKey,...categories.map(c=>[categoryKey(c.name),c.count])]);if(chips.dataset.firebaseCategorySignature===signature){chips.classList.add('bookora-category-ready');return;}chips.dataset.firebaseCategorySignature=signature;chips.innerHTML=`<button type="button" class="${selectedKey?'':'active'}" data-category-chip="">All Categories</button>`+categories.map(c=>`<button type="button" class="${selectedKey===categoryKey(c.name)?'active':''}" data-category-chip="${escapeHTML(c.name)}">${escapeHTML(c.name)}</button>`).join('');chips.classList.add('bookora-category-ready');chips.querySelectorAll('[data-category-chip]').forEach(btn=>{if(btn.dataset.firebaseCategoryBound==='1')return;btn.dataset.firebaseCategoryBound='1';btn.addEventListener('click',()=>{const value=btn.getAttribute('data-category-chip')||'';const radio=[...p.querySelectorAll('input[name="filter-category"]')].find(r=>r.value===value);if(radio){radio.checked=true;radio.dispatchEvent(new Event('change',{bubbles:true}));}else{const base=window.location.hash.split('?')[0]||'#/explore';window.location.hash=value?`${base}?category=${encodeURIComponent(value)}`:base;}});});};

  const triggerActiveFilter=()=>{const p=page();if(!p)return;const selected=p.querySelector('input[name="filter-rating"]:checked');if(selected)selected.dispatchEvent(new Event('change',{bubbles:true}));};
  const refresh=()=>{if(!page())return;normalizeCatalog();removeUnwantedAI();updatePriceControls();styleRatingFilter();syncAvailableCategoryChips();triggerActiveFilter();};
  window.addEventListener('bookora:catalog-updated',()=>requestAnimationFrame(refresh),{passive:true});
  window.addEventListener('hashchange',()=>setTimeout(refresh,0));
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(refresh,0),{once:true});else setTimeout(refresh,0);
  const observer=new MutationObserver(()=>{if(page()){removeUnwantedAI();updatePriceControls();styleRatingFilter();syncAvailableCategoryChips();}});observer.observe(document.body,{childList:true,subtree:true});
})();
