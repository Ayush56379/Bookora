// Legacy floating role switcher is intentionally disabled.
// Account/workspace changes are now managed only from Account Settings.
export function renderRoleSwitcher() {
  return '';
}

export function initRoleSwitcherEvents() {
  // Intentionally no-op. Never switch account role from a floating control.
}
