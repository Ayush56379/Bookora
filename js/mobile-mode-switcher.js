// Mobile mode switcher removed.
// Mode selection is no longer exposed through the mobile drawer.
// Keep this file as a no-op because index.html may still reference it from
// older deployments/cached pages; it must never inject a mode-control UI.
export {};