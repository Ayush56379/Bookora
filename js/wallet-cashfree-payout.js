/* Bookora Wallet -> Firebase-authenticated Cashfree payout bridge. */
import { state } from './state.js';
const API = window.BOOKORA_API_URL || 'https://bookora-backend-x08l.onrender.com';
let wired = false;
async function firebaseToken(){const user=window.firebase?.auth?.().currentUser;if(!user)throw new Error('Please sign in again.');return user.getIdToken(true);}
async function api(path,options={}){const token=await firebaseToken();const headers=new Headers(options.headers||{});headers.set('Accept','application/json');headers.set('Authorization',`Bearer ${token}`);if(options.body&&!headers.has('Content-Type'))headers.set('Content-Type','application/json');const res=await fetch(API+path,{...options,headers});let data={};try{data=await res.json();}catch(_){}if(!res.ok)throw new Error(data.error||data.message||`Request failed (${res.status})`);return data;}
function toast(message,type='info'){try{window.Toast?.show?.(message,type);return;}catch(_){}try{window.dispatchEvent(new CustomEvent('bookora:toast',{detail:{message,type}}));}catch(_){}
}
function injectPayoutFields(){const form=document.getElementById('wallet-withdraw-form');if(!form||form.dataset.payoutEnhanced)return;form.dataset.payoutEnhanced='1';const method=document.getElementById('wallet-method');const destination=document.getElementById('wallet-destination');const bank=document.createElement('div');bank.id='wallet-bank-fields';bank.innerHTML='<label>Account holder<input id="wallet-account-holder" autocomplete="name" placeholder="Account holder name"></label><label>Bank account number<input id="wallet-account-number" inputmode="numeric" autocomplete="off" placeholder="Bank account number"></label><label>IFSC<input id="wallet-ifsc" autocapitalize="characters" placeholder="e.g. SBIN0001234"></label>';const submit=form.querySelector('button[type="submit"]');submit?.parentNode?.insertBefore(bank,submit);const toggle=()=>{const isBank=method?.value==='BANK';bank.style.display=isBank?'grid':'none';const label=destination?.closest('label');if(label)label.style.display=isBank?'none':'';};method?.addEventListener('change',toggle);toggle();}
async function loadProfile(){try{const result=await api('/api/payouts/profile');const p=result.profile||{};const upi=document.getElementById('wallet-destination');if(upi&&p.upiId)upi.value=p.upiId;const ifsc=document.getElementById('wallet-ifsc');const holder=document.getElementById('wallet-account-holder');if(ifsc&&p.ifsc)ifsc.value=p.ifsc;if(holder&&p.name)holder.value=p.name;const label=document.getElementById('wallet-upi-label');if(label)label.textContent=p.upiId||p.bankAccountMasked||'Not added';}catch(e){console.warn('Payout profile load:',e);}}
async function saveProfile(){const payload={accountHolder:document.getElementById('wallet-account-holder')?.value.trim()||'',accountNumber:document.getElementById('wallet-account-number')?.value.trim()||'',ifsc:document.getElementById('wallet-ifsc')?.value.trim().toUpperCase()||'',upiId:document.getElementById('wallet-destination')?.value.trim()||''};if(!payload.accountHolder&&!payload.upiId&&!payload.accountNumber)return;await api('/api/payouts/profile',{method:'POST',body:JSON.stringify(payload)});}
async function submitPayout(event){event.preventDefault();const amount=Number(document.getElementById('wallet-amount')?.value||0);const method=String(document.getElementById('wallet-method')?.value||'UPI').toUpperCase();if(amount<100)return toast('Minimum withdrawal is ₹100.','warning');try{const form=event.currentTarget;const button=form.querySelector('button[type="submit"]');if(button){button.disabled=true;button.dataset.originalText=button.textContent;button.textContent='Verifying & sending…';}await saveProfile();const result=await api('/api/payouts/request',{method:'POST',body:JSON.stringify({amount,method})});if(result.status==='unknown_pending')toast('Cashfree is checking the payout. Do not submit again; check status shortly.','warning');else toast('Withdrawal sent to Cashfree successfully.','success');setTimeout(()=>location.reload(),900);}catch(e){toast(e.message||'Withdrawal could not be processed.','error');}finally{const button=event.currentTarget?.querySelector('button[type="submit"]');if(button){button.disabled=false;button.textContent=button.dataset.originalText||'Request withdrawal →';}}}
function wire(){if(wired)return;const form=document.getElementById('wallet-withdraw-form');if(!form)return;wired=true;injectPayoutFields();form.addEventListener('submit',submitPayout,true);document.getElementById('wallet-save-upi')?.addEventListener('click',async()=>{try{await saveProfile();toast('Payout details saved securely.','success');await loadProfile();}catch(e){toast(e.message||'Unable to save payout details.','error');}},true);loadProfile();}
function observe(){wire();if(!document.getElementById('wallet-withdraw-form')){const observer=new MutationObserver(()=>{if(document.getElementById('wallet-withdraw-form')){observer.disconnect();wire();}});observer.observe(document.getElementById('app')||document.body,{childList:true,subtree:true});}}
window.addEventListener('hashchange',()=>{wired=false;setTimeout(observe,50);});state.subscribe(event=>{if(['USER_LOGGED_IN','DATA_SYNCED'].includes(event)){wired=false;setTimeout(observe,100);}});setTimeout(observe,300);
